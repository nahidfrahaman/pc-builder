/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.trustedreviews.com', 'images.pexels.com' , 'upload.wikimedia.org'],
  },
}

module.exports = nextConfig
